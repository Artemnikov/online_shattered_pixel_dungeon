"""Per-class talent data.

Each hero class owns a module here holding its Talent enum slice, definitions,
class requirements, titles and descriptions. `subclasses.py` merges them into
the global `Talent` enum and TALENT_* tables.
"""
